# Data Model: Dashboard UI Phase 1

No data entities are introduced in this phase. The dashboard shell is a pure layout/UI
feature with no database interaction. Data entities (Item, Collection, ItemType, etc.)
are defined in `src/lib/mock-data.ts` for future phases and remain unchanged.

This file is retained as a placeholder so the spec directory structure remains consistent
with the standard speckit workflow.
